name: Verify
on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    name: Build
    uses: ./.github/workflows/verify.yaml
    secrets: inherit

  test:
    name: Test E2E
    uses: ./.github/workflows/tests-e2e.yaml
    secrets: inherit
